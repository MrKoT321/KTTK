import { ObjectImageType } from '../types/types'

const ImageObject = (props: ObjectImageType) => {
    const i = 0
    return (
        <div>
            <div>Image</div>
        </div>
    )
}

export { ImageObject }
