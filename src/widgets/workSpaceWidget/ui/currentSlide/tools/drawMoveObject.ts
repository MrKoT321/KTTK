import { ObjectType } from "../../../../../shared/types/types"

type DrawMoveObjectParams = {
    object: ObjectType
    width: number
    height: number
}

const drawMoveObject = ({object, width, height}: DrawMoveObjectParams) => {

}

export {drawMoveObject}